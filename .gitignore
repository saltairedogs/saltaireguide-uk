# -----------------------------------------------------------------------------
# Saltaire Guide — hardened .gitignore (Next.js / TS / Vercel)
# Goal: reduce accidental commits of secrets, keys, local artifacts, and caches.
# -----------------------------------------------------------------------------

# ---- Dependencies ----
node_modules/
.pnp
.pnp.*
.pnpm-store/
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Optional: if using Bun locally
.bun/
bun-cache/
# (Keep bun.lockb committed if you use Bun; do NOT ignore bun.lockb)

# ---- Build outputs / frameworks ----
.next/
out/
dist/
build/
coverage/
.nyc_output/
.turbo/
.swc/
.cache/
.parcel-cache/
.eslintcache
.stylelintcache

# Storybook / docs builds (if used)
storybook-static/
.docusaurus/
.svelte-kit/

# ---- Testing artifacts ----
playwright-report/
test-results/
cypress/videos/
cypress/screenshots/
reports/
junit.xml

# ---- Logs ----
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ---- Local env / dotenv (never commit secrets) ----
.env
.env.*
.envrc
.direnv/
# Allow safe templates/examples to be committed
!.env.example
!.env.sample
!.env.template
!.env.dist
# If you keep per-environment examples, allow these patterns too
!.env.*.example
!.env.*.sample
!.env.*.template

# ---- Vercel / Netlify / platform metadata ----
.vercel/
.netlify/

# ---- Next.js / TypeScript ----
*.tsbuildinfo
next-env.d.ts

# ---- OS / editor junk ----
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini

# VS Code (ignore most; allow shareable workspace settings if you want)
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json

# JetBrains
.idea/
*.iml

# ---- Local data / DBs / dumps ----
data/
tmp/
temp/
*.sqlite
*.sqlite3
*.db
*.bak
*.dump
*.sql
*.tar
*.gz
*.zip

# ---- Keys / certificates / credentials (high risk) ----
# Private keys & certs
*.pem
*.key
*.crt
*.cer
*.p7b
*.p7c
*.pfx
*.p12
*.jks
*.keystore

# SSH keys / known sensitive filenames
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
*.ppk

# Common secret stores
*.kdbx
*.gpg
*.asc

# Cloud/provider credential files accidentally dropped into repo
.aws/
**/.aws/
gcloud/
**/gcloud/
service-account*.json
*serviceaccount*.json
*firebase-adminsdk*.json
google-credentials*.json

# NPM auth tokens (often stored here)
.npmrc
.npmrc.*
# If you need a committed .npmrc (rare), replace the above with:
# .npmrc.local
# .npmrc.*.local

# Supabase local state (if you use Supabase CLI)
.supabase/
supabase/.temp/
supabase/.branches/

# Generic “secrets” folders/files
secrets/
private/
certs/
keys/
**/*secret*
**/*.secrets.*
**/*.secret.*

# ---- Misc ----
*.orig
*.rej
*.swp
*.swo
*~
